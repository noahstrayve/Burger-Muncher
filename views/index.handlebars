<h1>BURGER TIME</h1>

<h2>Burgers that have been eaten</h2>

<ul>
  {{#each burgers}}
    {{#unless eaten}}
      {{> burgers/burger-buttons isEaten=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Burgers that are ready to serve</h2>

<ul>
  {{#each burgers}}
    {{#if eaten}}
      {{> burgers/burger-buttons isEaten=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-burger">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="ca" name="name">
  </div>

  <div class="form-group">
    <label for="eaten">Have youe eaten this burger?</label><br>
    <input type="radio" name="eaten" value="1" checked>It's ready to serve<br>
    <input type="radio" name="eaten" value="0">I've eaten it
  </div>

  <button type="submit">Add Burger</button>
</form>
